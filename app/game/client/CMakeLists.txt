cmake_minimum_required(VERSION 3.7)

FILE(GLOB_RECURSE CLIENT_FILES ./*.h ./*.cc ./*.hpp ./*.cpp)

ADD_EXECUTABLE(Client 
	${CLIENT_FILES}
)

TARGET_LINK_LIBRARIES(Client GAME_PROTO TONY_CAT BASE_PROTO ${LIB_DEPEND})
