cmake_minimum_required(VERSION 3.7)

FILE(GLOB_RECURSE GATE_SERVER_FILES ./*.h ./*.cc ./*.hpp ./*.cpp)

ADD_EXECUTABLE(GateServer 
	${GATE_SERVER_FILES}
)

TARGET_LINK_LIBRARIES(GateServer TONY_CAT PROTO ${LIB_DEPEND})
